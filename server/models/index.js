// Importing model schemas
const User = require('./User');
const Book = require('./Book');

// Exporting models as an object for easy access in other parts of the application
module.exports = {
    User,
    Book,
 };
 
